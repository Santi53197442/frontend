{
  "name": "pro2",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0", // Esta versión es muy alta, la última estable para testing-library/react es ~15.x.x con React 18. Con React 19 podría ser diferente o no estar completamente soportada.
    "@testing-library/user-event": "^14.5.2", // Actualizada a una versión común con React 18/CRA
    "antd": "^5.19.3", // Última versión estable de antd v5 (verifica la actual)
    "axios": "^1.7.2",
    "react": "^19.1.0", // Manteniendo React 19
    "react-dom": "^19.1.0", // Manteniendo React DOM 19
    "react-router-dom": "^6.25.1", // Última versión estable de react-router-dom v6 (verifica la actual)
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4" // Podrías actualizar a v3.x si es compatible con React 19 y CRA 5
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}